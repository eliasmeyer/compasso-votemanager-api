<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <property name="LOG_DIR" value="./logs"/>
  <property name="LOG_NAME" value="compasso-votemanager-api"/>

  <appender class="ch.qos.logback.core.ConsoleAppender" name="CONSOLE">
    <encoder>
      <pattern>
        %d{dd-MM-yyyy HH:mm:ss.SSS} %magenta([%thread]) %mdc %highlight(%-5level) %logger{36}.%M -
        %msg%n
      </pattern>
    </encoder>
  </appender>
  <appender class="ch.qos.logback.core.rolling.RollingFileAppender" name="FILE">
    <encoder>
      <pattern>%d{dd-MM-yyyy HH:mm:ss.SSS} %magenta([%thread]) %mdc %highlight(%-5level)
        %logger{36}.%M - %msg%n
      </pattern>
    </encoder>
    <file>${LOG_DIR}/${LOG_NAME}.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>${LOG_DIR}/${LOG_NAME}.%d{yyyy-MM-dd}.gz</fileNamePattern>
      <maxHistory>15</maxHistory>
      <totalSizeCap>1GB</totalSizeCap>
    </rollingPolicy>
  </appender>

  <logger name="br.com.compasso" additivity="false" level="debug">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="FILE"/>
  </logger>
  <root level="info">
    <appender-ref ref="CONSOLE"/>
  </root>
</configuration>
